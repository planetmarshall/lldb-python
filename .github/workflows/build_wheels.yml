name: Build

on: 
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }} for ${{ matrix.python_version }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # macos-13 is an intel runner, macos-14 is apple silicon
        os: [macos-14]
        python_version: ["cp310-*", "cp311-*", "cp312-*"]
        include:
          # - os: ubuntu-latest
          # SKBUILD_LLVM_TARGETS_TO_BUILD: "LLVM_ENABLE_TARGETS=X86"
          - os: macos-14
            llvm_targets: "AArch64"
            llvm_runtimes: "libcxx"

    steps:
      - uses: actions/checkout@v4

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.17.0
        env:
          SKBUILD_LLVM_TARGETS_TO_BUILD: ${{ matrix.llvm_targets }}
          SKBUILD_LLVM_ENABLE_RUNTIMES: ${{ matrix.llvm_runtimes }}
          CIBW_BUILD: ${{ matrix.python_version }}

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl
